// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"mocku/backend/ent/careers"
	"mocku/backend/ent/predicate"
	"mocku/backend/ent/professor"
	"mocku/backend/ent/subject"
	"mocku/backend/ent/users"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProfessorQuery is the builder for querying Professor entities.
type ProfessorQuery struct {
	config
	ctx              *QueryContext
	order            []professor.OrderOption
	inters           []Interceptor
	predicates       []predicate.Professor
	withUser         *UsersQuery
	withBoss         *ProfessorQuery
	withSubordinates *ProfessorQuery
	withSubjects     *SubjectQuery
	withCareers      *CareersQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProfessorQuery builder.
func (pq *ProfessorQuery) Where(ps ...predicate.Professor) *ProfessorQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit the number of records to be returned by this query.
func (pq *ProfessorQuery) Limit(limit int) *ProfessorQuery {
	pq.ctx.Limit = &limit
	return pq
}

// Offset to start from.
func (pq *ProfessorQuery) Offset(offset int) *ProfessorQuery {
	pq.ctx.Offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *ProfessorQuery) Unique(unique bool) *ProfessorQuery {
	pq.ctx.Unique = &unique
	return pq
}

// Order specifies how the records should be ordered.
func (pq *ProfessorQuery) Order(o ...professor.OrderOption) *ProfessorQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryUser chains the current query on the "user" edge.
func (pq *ProfessorQuery) QueryUser() *UsersQuery {
	query := (&UsersClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(professor.Table, professor.FieldID, selector),
			sqlgraph.To(users.Table, users.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, professor.UserTable, professor.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBoss chains the current query on the "boss" edge.
func (pq *ProfessorQuery) QueryBoss() *ProfessorQuery {
	query := (&ProfessorClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(professor.Table, professor.FieldID, selector),
			sqlgraph.To(professor.Table, professor.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, professor.BossTable, professor.BossColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySubordinates chains the current query on the "subordinates" edge.
func (pq *ProfessorQuery) QuerySubordinates() *ProfessorQuery {
	query := (&ProfessorClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(professor.Table, professor.FieldID, selector),
			sqlgraph.To(professor.Table, professor.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, professor.SubordinatesTable, professor.SubordinatesColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySubjects chains the current query on the "subjects" edge.
func (pq *ProfessorQuery) QuerySubjects() *SubjectQuery {
	query := (&SubjectClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(professor.Table, professor.FieldID, selector),
			sqlgraph.To(subject.Table, subject.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, professor.SubjectsTable, professor.SubjectsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCareers chains the current query on the "careers" edge.
func (pq *ProfessorQuery) QueryCareers() *CareersQuery {
	query := (&CareersClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(professor.Table, professor.FieldID, selector),
			sqlgraph.To(careers.Table, careers.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, professor.CareersTable, professor.CareersColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Professor entity from the query.
// Returns a *NotFoundError when no Professor was found.
func (pq *ProfessorQuery) First(ctx context.Context) (*Professor, error) {
	nodes, err := pq.Limit(1).All(setContextOp(ctx, pq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{professor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *ProfessorQuery) FirstX(ctx context.Context) *Professor {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Professor ID from the query.
// Returns a *NotFoundError when no Professor ID was found.
func (pq *ProfessorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(1).IDs(setContextOp(ctx, pq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{professor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *ProfessorQuery) FirstIDX(ctx context.Context) int {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Professor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Professor entity is found.
// Returns a *NotFoundError when no Professor entities are found.
func (pq *ProfessorQuery) Only(ctx context.Context) (*Professor, error) {
	nodes, err := pq.Limit(2).All(setContextOp(ctx, pq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{professor.Label}
	default:
		return nil, &NotSingularError{professor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *ProfessorQuery) OnlyX(ctx context.Context) *Professor {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Professor ID in the query.
// Returns a *NotSingularError when more than one Professor ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *ProfessorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(2).IDs(setContextOp(ctx, pq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{professor.Label}
	default:
		err = &NotSingularError{professor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *ProfessorQuery) OnlyIDX(ctx context.Context) int {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Professors.
func (pq *ProfessorQuery) All(ctx context.Context) ([]*Professor, error) {
	ctx = setContextOp(ctx, pq.ctx, "All")
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Professor, *ProfessorQuery]()
	return withInterceptors[[]*Professor](ctx, pq, qr, pq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pq *ProfessorQuery) AllX(ctx context.Context) []*Professor {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Professor IDs.
func (pq *ProfessorQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pq.ctx.Unique == nil && pq.path != nil {
		pq.Unique(true)
	}
	ctx = setContextOp(ctx, pq.ctx, "IDs")
	if err = pq.Select(professor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *ProfessorQuery) IDsX(ctx context.Context) []int {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *ProfessorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pq.ctx, "Count")
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pq, querierCount[*ProfessorQuery](), pq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pq *ProfessorQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *ProfessorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pq.ctx, "Exist")
	switch _, err := pq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *ProfessorQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProfessorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *ProfessorQuery) Clone() *ProfessorQuery {
	if pq == nil {
		return nil
	}
	return &ProfessorQuery{
		config:           pq.config,
		ctx:              pq.ctx.Clone(),
		order:            append([]professor.OrderOption{}, pq.order...),
		inters:           append([]Interceptor{}, pq.inters...),
		predicates:       append([]predicate.Professor{}, pq.predicates...),
		withUser:         pq.withUser.Clone(),
		withBoss:         pq.withBoss.Clone(),
		withSubordinates: pq.withSubordinates.Clone(),
		withSubjects:     pq.withSubjects.Clone(),
		withCareers:      pq.withCareers.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProfessorQuery) WithUser(opts ...func(*UsersQuery)) *ProfessorQuery {
	query := (&UsersClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withUser = query
	return pq
}

// WithBoss tells the query-builder to eager-load the nodes that are connected to
// the "boss" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProfessorQuery) WithBoss(opts ...func(*ProfessorQuery)) *ProfessorQuery {
	query := (&ProfessorClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withBoss = query
	return pq
}

// WithSubordinates tells the query-builder to eager-load the nodes that are connected to
// the "subordinates" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProfessorQuery) WithSubordinates(opts ...func(*ProfessorQuery)) *ProfessorQuery {
	query := (&ProfessorClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withSubordinates = query
	return pq
}

// WithSubjects tells the query-builder to eager-load the nodes that are connected to
// the "subjects" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProfessorQuery) WithSubjects(opts ...func(*SubjectQuery)) *ProfessorQuery {
	query := (&SubjectClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withSubjects = query
	return pq
}

// WithCareers tells the query-builder to eager-load the nodes that are connected to
// the "careers" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProfessorQuery) WithCareers(opts ...func(*CareersQuery)) *ProfessorQuery {
	query := (&CareersClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withCareers = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		IdentityCard string `json:"identity_card,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Professor.Query().
//		GroupBy(professor.FieldIdentityCard).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *ProfessorQuery) GroupBy(field string, fields ...string) *ProfessorGroupBy {
	pq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProfessorGroupBy{build: pq}
	grbuild.flds = &pq.ctx.Fields
	grbuild.label = professor.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		IdentityCard string `json:"identity_card,omitempty"`
//	}
//
//	client.Professor.Query().
//		Select(professor.FieldIdentityCard).
//		Scan(ctx, &v)
func (pq *ProfessorQuery) Select(fields ...string) *ProfessorSelect {
	pq.ctx.Fields = append(pq.ctx.Fields, fields...)
	sbuild := &ProfessorSelect{ProfessorQuery: pq}
	sbuild.label = professor.Label
	sbuild.flds, sbuild.scan = &pq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProfessorSelect configured with the given aggregations.
func (pq *ProfessorQuery) Aggregate(fns ...AggregateFunc) *ProfessorSelect {
	return pq.Select().Aggregate(fns...)
}

func (pq *ProfessorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pq); err != nil {
				return err
			}
		}
	}
	for _, f := range pq.ctx.Fields {
		if !professor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *ProfessorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Professor, error) {
	var (
		nodes       = []*Professor{}
		withFKs     = pq.withFKs
		_spec       = pq.querySpec()
		loadedTypes = [5]bool{
			pq.withUser != nil,
			pq.withBoss != nil,
			pq.withSubordinates != nil,
			pq.withSubjects != nil,
			pq.withCareers != nil,
		}
	)
	if pq.withUser != nil || pq.withBoss != nil || pq.withCareers != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, professor.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Professor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Professor{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withUser; query != nil {
		if err := pq.loadUser(ctx, query, nodes, nil,
			func(n *Professor, e *Users) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := pq.withBoss; query != nil {
		if err := pq.loadBoss(ctx, query, nodes, nil,
			func(n *Professor, e *Professor) { n.Edges.Boss = e }); err != nil {
			return nil, err
		}
	}
	if query := pq.withSubordinates; query != nil {
		if err := pq.loadSubordinates(ctx, query, nodes,
			func(n *Professor) { n.Edges.Subordinates = []*Professor{} },
			func(n *Professor, e *Professor) { n.Edges.Subordinates = append(n.Edges.Subordinates, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withSubjects; query != nil {
		if err := pq.loadSubjects(ctx, query, nodes,
			func(n *Professor) { n.Edges.Subjects = []*Subject{} },
			func(n *Professor, e *Subject) { n.Edges.Subjects = append(n.Edges.Subjects, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withCareers; query != nil {
		if err := pq.loadCareers(ctx, query, nodes, nil,
			func(n *Professor, e *Careers) { n.Edges.Careers = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *ProfessorQuery) loadUser(ctx context.Context, query *UsersQuery, nodes []*Professor, init func(*Professor), assign func(*Professor, *Users)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Professor)
	for i := range nodes {
		if nodes[i].professor_user == nil {
			continue
		}
		fk := *nodes[i].professor_user
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(users.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "professor_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pq *ProfessorQuery) loadBoss(ctx context.Context, query *ProfessorQuery, nodes []*Professor, init func(*Professor), assign func(*Professor, *Professor)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Professor)
	for i := range nodes {
		if nodes[i].professor_subordinates == nil {
			continue
		}
		fk := *nodes[i].professor_subordinates
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(professor.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "professor_subordinates" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pq *ProfessorQuery) loadSubordinates(ctx context.Context, query *ProfessorQuery, nodes []*Professor, init func(*Professor), assign func(*Professor, *Professor)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Professor)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Professor(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(professor.SubordinatesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.professor_subordinates
		if fk == nil {
			return fmt.Errorf(`foreign-key "professor_subordinates" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "professor_subordinates" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *ProfessorQuery) loadSubjects(ctx context.Context, query *SubjectQuery, nodes []*Professor, init func(*Professor), assign func(*Professor, *Subject)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*Professor)
	nids := make(map[int]map[*Professor]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(professor.SubjectsTable)
		s.Join(joinT).On(s.C(subject.FieldID), joinT.C(professor.SubjectsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(professor.SubjectsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(professor.SubjectsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Professor]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Subject](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "subjects" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (pq *ProfessorQuery) loadCareers(ctx context.Context, query *CareersQuery, nodes []*Professor, init func(*Professor), assign func(*Professor, *Careers)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Professor)
	for i := range nodes {
		if nodes[i].careers_leader == nil {
			continue
		}
		fk := *nodes[i].careers_leader
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(careers.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "careers_leader" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pq *ProfessorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	_spec.Node.Columns = pq.ctx.Fields
	if len(pq.ctx.Fields) > 0 {
		_spec.Unique = pq.ctx.Unique != nil && *pq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *ProfessorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(professor.Table, professor.Columns, sqlgraph.NewFieldSpec(professor.FieldID, field.TypeInt))
	_spec.From = pq.sql
	if unique := pq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pq.path != nil {
		_spec.Unique = true
	}
	if fields := pq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, professor.FieldID)
		for i := range fields {
			if fields[i] != professor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *ProfessorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(professor.Table)
	columns := pq.ctx.Fields
	if len(columns) == 0 {
		columns = professor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.ctx.Unique != nil && *pq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProfessorGroupBy is the group-by builder for Professor entities.
type ProfessorGroupBy struct {
	selector
	build *ProfessorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *ProfessorGroupBy) Aggregate(fns ...AggregateFunc) *ProfessorGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the selector query and scans the result into the given value.
func (pgb *ProfessorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgb.build.ctx, "GroupBy")
	if err := pgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProfessorQuery, *ProfessorGroupBy](ctx, pgb.build, pgb, pgb.build.inters, v)
}

func (pgb *ProfessorGroupBy) sqlScan(ctx context.Context, root *ProfessorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pgb.flds)+len(pgb.fns))
		for _, f := range *pgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProfessorSelect is the builder for selecting fields of Professor entities.
type ProfessorSelect struct {
	*ProfessorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ps *ProfessorSelect) Aggregate(fns ...AggregateFunc) *ProfessorSelect {
	ps.fns = append(ps.fns, fns...)
	return ps
}

// Scan applies the selector query and scans the result into the given value.
func (ps *ProfessorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ps.ctx, "Select")
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProfessorQuery, *ProfessorSelect](ctx, ps.ProfessorQuery, ps, ps.inters, v)
}

func (ps *ProfessorSelect) sqlScan(ctx context.Context, root *ProfessorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ps.fns))
	for _, fn := range ps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
